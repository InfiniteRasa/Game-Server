[MYSQL]
# Put the data for your mysql database connection here
# Also make sure the database is set up properly with the correct tables (import tabuladb.sql)

# the host name, usually 'localhost'
dbHost = localhost
dbPort = 10061
# the name of the database
dbName = infinite rasa

# the user
dbUser = root

# the password
dbPass = usbw
